# Today

`Today` возвращает текущую дату.

## Today()

`Today()` возвращает текущую дату со временем `00:00:00`.

Примеры:

| Expression | Result |
| --- | --- |
| `Today().Hour()` | `0` |
| `Today().Minute()` | `0` |
| `Today().Second()` | `0` |
